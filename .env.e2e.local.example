# Template for .env.e2e.local — copy to .env.e2e.local and fill in.
# .env.e2e.local is gitignored (matched by .env.*.local).
# Only needed when running `npm run seed:e2e` — never used by the app itself.

SUPABASE_URL_E2E=https://hfewsknqnlwphaeamvuv.supabase.co
SUPABASE_SERVICE_ROLE_KEY_E2E=

# Optional — override documented defaults in scripts/seed-e2e-users.ts.
# If you override these, also update the matching GitHub secrets for CI.
# E2E_ADMIN_PASSWORD=
# E2E_DRIVER_PASSWORD=
# E2E_CLIENT_PASSWORD=
